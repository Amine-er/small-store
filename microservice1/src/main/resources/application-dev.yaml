spring:
  config:
    import: "configserver:"
  application:
    name: microservice1
  cloud:
    config:
      uri: http://localhost:8888
      name: microservice1
      fail-fast: true
      retry:
        initial-interval: 3000
        multiplier: 1.3
        max-interval: 10000
        max-attempts: 20